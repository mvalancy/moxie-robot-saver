# 🗂️ Backlog briefs — a ranked line, turned into something a build agent can execute

The [OpenMoxie feature audit](../openmoxie-feature-audit.md) ranks *what* to build (§4.1 Top-10 ADOPT,
§4.2 Top-10 BEYOND) and tracks *what has shipped* (the **Status** column in both tables). A ranked line is
a decision, though — not a plan. When an item's next step is big enough that a build agent would otherwise
have to re-derive the design, it gets a **brief** here.

## What a brief contains

Every page in this folder answers the same questions, in this order, so an agent can start at the top and
work down:

1. **Goal** — one paragraph, and what "done" looks like from the child's side.
2. **The seam** — the exact file, function and call sites it plugs into today, and what that costs.
3. **The vocabularies** — the recovered ids it may emit, each cited to a doc **and line range** in
   [`../../reverse-engineering/`](../../reverse-engineering/README.md). Nothing outside them reaches the wire.
4. **Prior art** — what [OpenMoxie](https://github.com/jbeghtol/openmoxie) (MIT) does, cited by path, and
   which **behaviors** we port. We describe and credit; we never copy code.
5. **The design** — pure and testable where it can be, with the invariants written down.
6. **Tests** — a numbered table, hermetic first.
7. **Acceptance criteria** — checkable, not aspirational.
8. **Effort, files to touch, risks** — including the limits we could *not* establish from our own docs.

## The briefs

| Brief | Audit items | Effort | State — live, **2026-09-06** *(was "live, 2026-09-03")* |
|---|---|:--:|---|
| [`turnstile-layout-collision.md`](turnstile-layout-collision.md) | **none** — a layout defect found while building PR #192, not an audit item | S · S · ? | 🟢 **fixed 2026-09-06** — the challenge now centres in the space **above** the bottom-anchored controls (`turnstile.js::place()`), not in the viewport; **arming Turnstile is no longer gated on it**. Re-derived: the collision window is **683 < vh < 909** (844/851/896 — *not* the 375×667 the filing measured), and the guard is `test_mobile_layout.mjs` **block 9**, which drives `#transcript` to its cap through `ambient.js`'s own seam. Cost: **0 px of transcript**. Left open: a **separate live-today `env.js` defect** the same driven state exposes — `#env-banner` lands on `#rail-toggle` at vh ≤ 740 with no Turnstile involved. |
| [`expressiveness.md`](expressiveness.md) | **ADOPT #3** — the markup floor (§1) · **BEYOND #1** — the behavior planner (§2) | M → L | §1 🟢 shipped 2026-09-02 · §2 🟢 **P1 SHIPPED 2026-09-03** *(was "🟢 build-ready" — stale for three days; the brief's own §2 heading said SHIPPED the whole time)*. Verified: [`markup.py`](../../../mqtt/supervisor/markup.py) is 191 lines and dispatches three generations on `MOXIE_EXPRESSIVE` (:3-10) — [`performance.py`](../../../mqtt/moxie_sdk/performance.py) (839 ln, the planner) and [`automarkup.py`](../../../mqtt/moxie_sdk/automarkup.py) (489 ln, the floor), behind [`test_performance.py`](../../../sim/tests/test_performance.py) + [`test_sil_performance_e2e.py`](../../../sim/tests/test_sil_performance_e2e.py). **§2's P2 open:** the ≥500 **live**-line unknown-id bar (audit §4.4 **#4**) |
| [`security-broker-auth.md`](security-broker-auth.md) | **§3.1 Robot identity / JWT** — broker ACL (P0), device credentials (P1), spoof-proofing (P2) | M · M · S | P0 🟢 **shipped 2026-09-02 (PR #44)** — `test_broker_acl.py` (30) + `run_acl_proof.sh`. P1/P2 ⛔ **blocked on A1–A4**, which need a physical robot (audit §4.4 #5) |
| [`telehealth.md`](telehealth.md) | **ADOPT #7** — puppet / telehealth: the command path and the "Be Moxie" console panel | M | 🟢 **shipped 2026-09-02 (PR #43)** — `test_telehealth*.py`, 110 tests |
| [`content-packs.md`](content-packs.md) | **ADOPT #5** — content packs: a versioned, digest-checked pack file; export from a positive field allowlist; import-with-review whose per-item state tracks `source_version` **and** local edits, so an upstream re-import never clobbers | S/M · S/M | 🟢 **shipped 2026-09-02 (PR #51) — P0 *and* P1**; hardened 2026-09-03 (PR #78). 160 tests |
| [`voice-picker.md`](voice-picker.md) | 🎚️ Speech + Listening dropdowns in the console — pick from the gateway's real audio models and the installed local engines; default `piper-amy` / `stt-whisper`; explicit local always wins | S/M | 🟢 **shipped 2026-09-02 (PR #48)**; corrected 2026-09-03 (PR #77 — an explicit `MOXIE_TTS`/`MOXIE_STT` **pins** the engine, so *"explicit local always wins"* is enforced, not merely intended) |
| [`live-sim-demo.md`](live-sim-demo.md) | 🌐 **The headline goal** — the hosted Moxie Sim alive on a static edge: three same-origin Cloudflare Pages Functions (brain · voice · ears) behind hard caps, demo-mode only, degrading to the pre-cached scripted Moxie when the gateway is unconfigured, over budget, at capacity or down | M | 🟡 **P0-a + P0-b shipped 2026-09-02 (PR #54, #61); the ears (PR #66) and the fallback voice (PR #69) 2026-09-03.** P1 is 🟠 **split, and ranked #5 in the audit as of 2026-09-04** — the TTS cache and the nonce CSP are build-ready, the **exact counters** are ⛔ blocked on §10 assumption 13 (*is KV or a Durable Object available on this plan?* — a dashboard question) and **Turnstile** ⛔ needs a sitekey minted on the account. A hardening pass against the live site landed 2026-09-03/04 (#107, #111, #112, #115, #122, #124). This page keeps its own state current — read its §10 ledger, not this cell |
| [`mobile-first-visit.md`](mobile-first-visit.md) | **Filed from production measurements, and the top live-page item.** On a 390 px phone the Talk box is `0 × 0` on load and sits at **y = 2 095** even after the `CONTROLS` drawer opens — while Moxie speaks unprompted at ~7 s, so a visitor hears her and cannot answer. It is **not broken**: tap `CONTROLS`, scroll ~2 000 px, and the turn completes. It is buried, and a demo link is opened on a phone more often than a desktop. | S/M | 🟢 **BUILT 2026-09-05** *(this row had **no Effort and no State cell at all** — 3 pipes in a 5-pipe table — so the index silently recorded nothing for a brief that had shipped; added 2026-09-06)*. [`sim/web/sim.html`](../../../sim/web/sim.html):322 `<div id="chat-dock">`, the `#chat-cue` line at :332 and `#speech-input` at :369, styled at [`style.css`](../../../sim/web/style.css):556 (`:has()` hides the cue once a turn exists, :586). The four controls were **moved, not copied** — `rail.js` byte-unchanged. Guard: [`sim/test_mobile_layout.mjs`](../../../sim/test_mobile_layout.mjs) (66 → 222). **Left open:** nobody has re-measured production after the deploy, so every number in the brief is the *defect*, not the current state |
| [`vendor-the-readme-hero.md`](vendor-the-readme-hero.md) | **Filed from a production measurement, not from the audit.** `docs.html` threw one `img-src` violation on every live load: the README's SIL hero was a `github.com/user-attachments` URL, and `img-src 'self' data: blob:` refuses it. Not a regression — the directive is byte-identical either side of PR #137, which only made it visible. Vendor the image rather than widening the CSP for one picture; the real work is that the bundle serves `README.md` from a different directory depth than GitHub does, so one relative path has to satisfy both. | S | 🟢 **SHIPPED 2026-09-04** — image vendored to `sim/web/img/sim-hero.png`; `docs.js` resolves a doc's `<img src>` against the doc's *repo* path and strips the `sim/web/` prefix (the Pages output root), so the one string renders in both places. `img-src` untouched. Guards: [`test_no_offsite_images.py`](../../../sim/tests/test_no_offsite_images.py) (8, incl. two planted negative controls), `sim/test_csp.mjs` 77 → 79 and `sim/test_docs_explorer.mjs` 22 → 25 — **3 red on each pre-change tree**, measured by stashing. |
| [`brain-picker.md`](brain-picker.md) | **BEYOND #3** — any brain, hot-swappable, per child: a closed positive registry of brains, per-robot selection layered `defaults ⊕ fleet ⊕ per-robot` like every other config value, a live swap with no restart, and an explicit `MOXIE_APP` that **pins** the appliance's brain | S/M | 🟢 **P0 SHIPPED 2026-09-03** — [`brains.py`](../../../mqtt/moxie_sdk/brains.py) behind [`test_brains.py`](../../../sim/tests/test_brains.py) (82) + [`test_brain_runtime.py`](../../../sim/tests/test_brain_runtime.py) (31) + [`test_brain_console.py`](../../../sim/tests/test_brain_console.py) (13), plus a 22-guard mutation run. Ships with the 🧠 console card. **P1 open:** the *persona* half of the binding, per-child keys/cost accounting (all three need a new secret) |
| [`sandboxed-extensions.md`](sandboxed-extensions.md) | **BEYOND #6** — sandboxed content extensions: a pack that can *do* something (count, check the clock, remember a score) without trusting its author. A declarative rule list over a total JSON-AST expression language — no `exec`, no parser, no loops, no reachable host object — behind a declared capability set the parent reads in plain English at import review | S/M · M · L | 🟢 **P0 SHIPPED 2026-09-03** — [`ext.py`](../../../mqtt/moxie_sdk/content/ext.py) behind [`test_ext_escapes.py`](../../../sim/tests/test_ext_escapes.py) (X1–X12) and [`test_ext.py`](../../../sim/tests/test_ext.py) (T1–T18), 150 tests, plus a 28-guard mutation run. All six upstream hooks hand-ported as the golden set; G1 ships as a real activity. **P1's `act` half SHIPPED 2026-09-04 (PR #121)** — S5 closed, [`test_ext_act.py`](../../../sim/tests/test_ext_act.py) added, and **2 of the 4 `xfail` conformance rows measured green, not 4** (G2/G3; G5 needs `brain`, G6 needs `subscribe`). **P1's `subscribe` SHIPPED 2026-09-05** *(this cell said it had "**no host**, since nothing joins `Volley.subscriptions` to `wire.build_chat_response(subscribe_events=…)`" — it does now:* [`volley.py`](../../../mqtt/moxie_sdk/content/volley.py):89, :100 `update_subscriptions`, :109 `add_subscriptions`, behind [`test_ext_subscribe.py`](../../../sim/tests/test_ext_subscribe.py) and [`subscribe_mutation_check.py`](../../../sim/tools/subscribe_mutation_check.py)*)*. **P1 still open:** `brain` (G5 alone is `xfail(strict)` until it exists), the parent-facing **grant flow** (without which `act` is reachable from tests and from nobody else), `turn.after`/`session.end`, the text surface, the JS evaluator in `workerd`, the console card. **P2 open:** a Wasm runtime, publisher signatures |
| [`production-hardening.md`](production-hardening.md) | **§4.4 #3** — production hardening: the cross-process store story (a decision ADOPT #8 deferred three times), MQTT reconnection, and *"stays connected for a week"* as a test we can run without hardware | M · M · L | 🟢 **P0 + P1 shipped 2026-09-03; P2 build-ready.** §3 **decided** the owed question — advisory `flock` on a per-record sidecar behind a public `JsonStore.transaction()`, JSON staying on disk — over WAL-SQLite and over a single-writer rule, and named what would flip it; neither build hit a falsifier. §4's reconnection shipped in P0 (including the trap that `connect_async` without `retry_first_connection=True` changes nothing). §5's soak shipped in P1 and **has run**: `sim/run_soak.sh`, three profiles, twelve numeric bars printed pass-or-fail — 1 046 turns with 0 lost while up, reconnect p95 0.62 s, 0 lost updates across 4 contending processes. Plus the durable roster, the connection telemetry stream, and the SIGTERM handler. The **12th** bar is the interesting one: it exists because a whole defect class (a cached belief about the robot outliving the robot's state — the roster ghost, the vision latch) sat underneath eleven green ones. **Six of its twenty-four assumptions need a physical robot** — the honest ceiling on this whole area, unmoved by either phase |
| [`content-authoring.md`](content-authoring.md) | **§4.4 #6** — content authoring, the verb packs did not ship: a parent can install a stranger's conversation and diff it against their own, and still cannot compose one without editing JSON. Where authoring lives (four options, one choice), what a non-programmer may and may not write, the edit → hear → keep loop priced in gateway calls, and one validation path rather than two | S/M · M · L | 🟡 **P0 SHIPPED 2026-09-04 — a parent can write one; P1 (the paid *try*) and P2 open** *(was 🟢 build-ready that morning)* — **P0 built 2026-09-04**: `POST /content/item` and `POST /content/render` on the supervisor, both proxied at `/local/content/*`, the editor panel on the 📦 card, `packs.shadow_check` for the name-order rule, and [`test_content_authoring.py`](../../../sim/tests/test_content_authoring.py) (21 tests) beside a 15-row mutation check. P1 (the paid *try*, its budget and the rehearse button) and P2 are open. The spec **removed all four owed decisions** (§11 checks itself against them): authoring lives in the **parent console**, as a second verb on the 📦 card, because it is the only surface that already holds the content store, the validation path *and* the rehearsal hook. The write seam was pre-declared in code — [`packs.mark_edited`](../../../mqtt/moxie_sdk/content/packs.py)'s docstring names *"a future 📦 edit button"* — and the review's 2×2 needs **no new state**, because `is_local_edited` already treats an item with no `imported_rev` as edited, so a later pack reports `CONFLICT` for free. The only new safety code in the whole brief is **one `if`**: `mark_edited` normalizes but does not validate, so the save route must call `validate_item` itself exactly as `apply_pack` does. The loop is five rungs and **exactly one spends a gateway call** — typing, the resolved-prompt panel and rehearsing an opener are all free; a *try* is one brain call per click, budgeted and counted on screen; trying a **command** costs nothing at all. **Three of its twelve assumptions need a real parent** (A9–A11) and one needs a robot (A8); none blocks P0 |
| [`insights.md`](insights.md) | **BEYOND #5** — insights that mean something: turn the ingested `Packet` history into a parent console (sessions, activity mix, mood trend, time-of-day, *"what did we talk about this week"*), all on-device. The blocker is a **vocabulary** — `Packet.event_name` is a free string and our corpus recovers no module-scoped events | M | 🟡 **partial — the durable half shipped (PR #55), the insight half is open** *(this 678-line brief was **not indexed here at all** until 2026-09-06 — the index's own gap, not a stale cell)*. Re-verified against the code: **no** `RESERVED_EVENTS`, `bucket_events` or `buckets` anywhere, no `_mint`, no `normalize_activity`, **no `sim/tests/test_insights.py`**. What exists is the roll-up — [`telemetry.py`](../../../mqtt/moxie_sdk/telemetry.py) `roll_up_packet`:380, `reconcile_rollup`:495, `history_view`:520 — over a still-free-string `event_name` (:105), with `MAX_DAY_EVENTS = 24` (:224) still capping **first-come** (the brief's T3, red-first). Two P0 rows *did* land out of band: `erase_telemetry` ([`moxie_runtime.py`](../../../mqtt/supervisor/moxie_runtime.py):3024, route :1215) and the P-1 `telemetry_policy` gate (:4880). **Ceiling:** nothing on this appliance has ever *produced* a `Packet`, so the store is correct, tested and **empty** |
| [`qr-launch-cards.md`](qr-launch-cards.md) | **§4.4 #9** — printable launch cards, **re-scoped**: the audit priced this an **S** on the claim that *"the action-tag parser already understands `<launch:MOD[:CID]>`"*, but that parser only ever reads a **brain's own reply text**, and a scanned card arrives from the other direction as `$eb_qr_value` on a vision turn nothing routes. Three pieces: arm the runtime reader, route the value into a launch against a closed 24-id allowlist, print the sheet | S · S/M · S (**M** together) | 🟡 **P0-b shipped 2026-09-04** — the route is built: [`launch_cards.py`](../../../mqtt/moxie_sdk/launch_cards.py) decodes `GO<launch:MOD>` to one typed `Action` against a **derived** 24-id allowlist (23 from `schedule.ONBOARD_MODULES` + `DM`, intersected with `DEFAULT_TEMPLATE` so it can only rot towards refusing), and `_on_vision_turn` puts it on the reply. 129 tests, 14/14 mutations caught. P0-a is half done, P0-c is not started. Spec'd 2026-09-03: The brief settles where the sheet lives (the console's 🎴 card), why the module allowlist is a *safety* property rather than tidiness, and the exact recovered wire shape for the arm (`ActionID.execute`, `function_id`, `function_args` — `RemoteChat.proto`:255-281), which also closes [`sandboxed-extensions.md`](sandboxed-extensions.md)'s **S5** blocker and flips its four `xfail(strict)` `act`/`subscribe` rows. **Honest ceiling:** no physical robot has ever sent us a vision event, so the end-to-end claim is unprovable on hardware whatever we build |
| [`smoke-load-sensitivity.md`](smoke-load-sensitivity.md) | 🔥 **The eleventh instance of the 2026-09-06 defect family, and the one under the INTEGRATION tier's own instrument.** `sim/run_smoke.sh` reddens when the box is busy: an **interleaved A/B** by the `clockbudget` agent measured pristine `origin/dev` at **6 of 7 OK** and its own branch at **6 of 9** — failures on *both* sides, with a diff touching none of the files the smoke loads. It is the layer above the two pytest budgets #206 fixed. It matters because this is the script that answers *"does a real turn round-trip through the built backend?"* every INTEGRATION fire, and **a gate that reddens when the box is busy teaches its reader to re-run it** — which is how the other ten stayed hidden. It has already caused one misread, an orchestrator recording a smoke failure as a merge regression before the A/B showed pristine `dev` failing identically. The brief insists the two causes be **separated**: a fixed wait standing in for a condition is the family's signature, but a genuine capacity limit in the runtime is a **product** finding and worth more | S–M (needs a quiet machine) | ⚪ **FILED 2026-09-06, not started — and deliberately not started.** The box was at load average 76–85 with two agents running; briefing a load-sensitivity investigation onto a thrashing machine would manufacture exactly the contaminated measurement this family exists to eliminate |
| [`head-travel-threshold.md`](head-travel-threshold.md) | 🎯 **The twelfth instance of the 2026-09-06 family — and the first this project introduced *while fixing that family*.** `sim/test_liveliness.mjs`'s block 4a asserts `spread > 40` px of head travel after waiting a **fixed four frames** per sweep; `animate()` clamps `dt` to 0.1 s, so a starved runner advances the arc *less*, and the check reads the shortfall as a defect in the drive. Block 4a arrived in **PR #203**, whose whole purpose was removing machine-dependent assertions. Measured by interleaved A/B at load 60–80: **red on pristine `dev` in 2 of 3 pairs** (30 px, 31 px) **and red on an unrelated branch too**, which is how it is known to belong to no branch | S (unknown until one stall is explained) | ⚪ **FILED 2026-09-07, not started — a fix was written and REVERTED.** Polling `getMotor(6)` for arrival failed with `motor 6 never reached 0 in 90 frames (stalled at 15910)`, while a standalone probe showed the same motor going 16384 → 0 in ~20 frames. That contradiction is unexplained, so the fix was reverted rather than shipped on a guess |
| [`ota-push.md`](ota-push.md) | **§3.1 OTA push (801→803 lever)** — re-verified 2026-09-06 as the cleanest genuinely-unbuilt row in the audit (`grep` for `ota_update\|forbid_otaver` in `mqtt/` + `server/` returns **zero**). The one item here that mutates **firmware**, on hardware nobody in this project owns, where a mistake is unrepairable without opening the shell | S · M · M (owner-gated) | ⛔ **SPECIFICATION ONLY — nothing built, deliberately, 2026-09-06.** The tree is unchanged apart from the brief and its two index entries; [`cloud_config.py`](../../../mqtt/moxie_sdk/cloud_config.py):102-115 still has no OTA keyword to pass. **Its §1 ledger labels every claim** PROVEN / SECOND-HAND / INFERRED / **UNKNOWN** — the whole wire flow is second-hand (OpenMoxie's `doc/RemoteModuleAPI.md`, prose not capture) and **four things are marked unknown**: what `forbid_otaver` means, whether a downgrade is refused, what `OTAStatus`'s bare integers mean (and whether that message reaches MQTT at all), and what the robot does with a URL it cannot fetch. **The refusals are designed before the happy path** — eight gates, and two of them exist because the seam already contains the hazard: [`_push_config`](../../../mqtt/supervisor/moxie_runtime.py):2517-2540 republishes the whole document **on every reconnect** (so a target stored as a setting is a permanent instruction), and `POST /config?scope=fleet` shares `sanitize_config_overrides` with the per-robot path ([`moxie_runtime.py`](../../../mqtt/supervisor/moxie_runtime.py):1399-1401), so one key on that whitelist is one POST from every robot. **Its §8 finding:** the brick risk is *not* a bad payload — `update_engine` rejects anything unsigned and writes only the inactive A/B slot — it is **defeating the signature gate**, which the brief forbids at every phase. **Its §9 recommends *specify, do not build***: there is no signed 803 image in this project to serve, and the population is 801/802 robots already re-homed to us, of which we know of none |
| [`visemes.md`](visemes.md) | **§4.4 #4 — `TTSMark[]` visemes** (the rest of **BEYOND #8**), which the audit asked for as *"a research slice, not a build agent"*. Answers its two open questions and re-frames the row | S · M · L | 🔵 **RESEARCH SLICE — nothing built, deliberately, 2026-09-06.** The tree is unchanged apart from the brief and its two index entries; `marks` is still empty at [`tts.py`](../../../mqtt/moxie_sdk/tts.py):378 and hardcoded `marks: []` at [`wire.js`](../../../functions/api/_lib/wire.js):132. **Q1 — can Piper emit the alignment? YES, exactly.** Measured on the build box: a stock `.onnx` has one output and drops the durations, but Piper ships `patch_voice_with_alignment`, which appends the VITS duration predictor's `Ceil`/`w_ceil` tensor as a second output; patched, 75 per-phoneme sample counts summed **exactly** to the 42 496 audio samples. Caveat: `noise_w = 0.8` makes them **stochastic**, so marks can never be regenerated and reattached to earlier audio. **Q2 — where marks come from post-#152:** cached *inside the same entry under the same key* (`v1`→`v2`), never regenerated (it would be precisely wrong) and never forced-aligned in production (`faster-whisper` is the test **oracle**, not a producer). **The finding that re-orders the work:** the browser consumer is *already built* — `VISEME_OPEN` and `markTrack` at [`audio.js`](../../../sim/web/audio.js):507-510, :551-566 — while the ceiling is elsewhere: the combine rule at :867 is `Math.max`, so a mark can only **open** her mouth, making bilabial closure (`p`/`b`/`m`, the most legible viseme there is) unrepresentable; and the face has **one** drivable degree of freedom ([`moxie.js`](../../../sim/web/moxie.js):1377, :1677-1681), so `/i/` and `/u/` are the same picture. **So §5 argues the row as written is ~1.2×, not 10×** — over the 12–20 visemes in a 1.7–3 s reply, on one scalar, an envelope is a *noisy proxy for the right signal*. The 10× is the renderer: a mouth that can close and round, drivable from the FFT the `AnalyserNode` already computes and throws away ([`audio.js`](../../../sim/web/audio.js):211, :462, :859 are all time-domain) — **no marks, no wire, no cache, no gateway**, and it works on the hosted demo where every producer option is unavailable. Tests are built as the mic clause was — **an ordering, not a magnitude** ([`sil-and-cicd.md`](../sil-and-cicd.md):360-385): discrimination + a five-way mutation suite, plus two **red-first** assertions that are unpassable by construction today. **Ceiling:** the `.onnx` weights are not in the repo, so CI can never verify live Piper alignment. Recommends **build P0, specify P1, do not build P2**; §1.2 also records **four more rotted citations** into the viseme consumer |
| [`community-signals.md`](community-signals.md) | **Not an audit item — the inbound half.** What owners holding a real Moxie report, cited by URL and date, ranked by evidence strength rather than appeal | — | 📡 **Scan 1, 2026-09-03.** Eight findings. One (**C1**, the CereProc licence) strengthens a blocked row in the audit; one (**C3**, IP drift after pairing) is a new build-ready gap on no other list; four are documentation corrections. **r/MoxieRobot was unreachable** — the scan's largest gap, named in its §4 |
| [`gamify-the-public-sim.md`](gamify-the-public-sim.md) | **Not an audit item, and not a design — evidence for the one part of the owner's chat-first steer that was left open.** *"Gamify this for regular people"* ([`../implementation-plan.md`](../implementation-plan.md):1598 records it as deliberately unspecified). Answers *who visits*, *what they loved*, *what disappoints* — each claim carrying a URL and a date — then prices **five** candidate directions with tests and a counter-argument each | — | 🔵 **Research brief, 2026-09-05 — awaiting the owner's steer, and it does NOT choose the game.** Two owner questions in its §0 unblock the build: *rescue on-ramp or meet-Moxie toy?* and *a game with a goal, or an effortless chat?* **Its §1 precondition CLOSED the same day it was filed** *(re-verified 2026-09-06)* — [`mobile-first-visit.md`](mobile-first-visit.md) shipped `#chat-dock` on 2026-09-05, so *"the Talk box is `0 × 0` on load and `y = 2 095` after `CONTROLS`"* is now the **defect**, not the current state. What remains of 🅐, still build-ready: **the three tappable openers** that send a turn — the existing `#speech-chips` ([`sim.html`](../../../sim/web/sim.html):182) are not those, they sit in the rail and play pre-cached shipped audio, as :174-178 says in its own words. Ranked: 🅐 reachable first turn (**S/M**, the precondition — *composer half shipped, openers half open*) → 🅒 knock-knock turn-taking (**S**, *recommended*) → 🅓 thinking-out-loud latency play (**S/M**) → 🅑 missions from the recovered `ONBOARD_MODULES` (**M**) → 🅔 on-ramp progress (**S/M**, defer). 🅒 and 🅓 are **behaviours credited to [`Noonster77/openmoxie`](https://github.com/Noonster77/openmoxie)** ("OpenMoxie Family Edition", [issue #63](https://github.com/jbeghtol/openmoxie/issues/63), 2026-08-21) — described and attributed, never copied. **Two honest holes it names rather than hides:** *no evidence exists, from anyone,* for first-30-seconds intent (and we ship **zero analytics**), and **r/MoxieRobot is structurally blocked to this agent** — `WebSearch` returns `400 … domains are not accessible to our user agent: ['reddit.com']`, so that gap is **owner-only or permanent**, an upgrade on `community-signals.md` §4's *"retry next scan"* |

> **Why this table is dated.** On 2026-09-03 two build agents were briefed off stale status markers and
> each lost a full run to work that had already merged. Four of the seven rows above said *"ready to
> build"* for something already shipped. **When a brief ships, flip its state here and in the audit in
> the same PR** — and prefer naming the test over naming the PR, because a test is checkable.
>
> **Re-verification, 2026-09-06 — and the drift ran one way again.** Every marker in all sixteen briefs
> was re-checked against the **code**, not against the audit and not against this table. **Six of the
> sixteen were stale, and all six understated what had shipped — none overstated it.** That is the same
> one-way drift PR #187 measured in the audit (21 of 54 rows, 39 %), one level down. The dangerous shape
> here is different from a stale audit row: `content-packs.md`:3 said **"Status: build-ready brief"**
> while the banner four lines below it said `✅ P0 shipped` and
> [`packs.py`](../../../mqtt/moxie_sdk/content/packs.py) had been 1 145 lines for four days. A brief
> announces itself as ready *and hands the reader a plan*. **This table was more accurate than the briefs
> it indexes** — so a build agent that opened the brief was worse informed than one that stopped here.

> **The last row is a different kind of page.** Every other brief turns a ranked line into something an
> agent can execute. `community-signals.md` does the opposite: it brings evidence *in* from outside this
> repo, so its rows are findings with URLs rather than designs with tests. It ranks by how good the
> evidence is, and it says plainly where a community problem is one we have already solved — which is a
> marketing finding, not an engineering one.

## House rules

- **Clean-room.** Derive from our own [reverse-engineering](../../reverse-engineering/README.md) and
  [architecture](../README.md) docs. OpenMoxie and its forks may be read and cited by path; never copied.
- **Honesty over completeness.** A brief says what it could not establish — an unverified robot behavior, a
  bundle-defined namespace, a missing hardware capture — in its own risks section, not in a footnote.
- **Cite by line.** `doc.md` lines 115–128 beats "see the markup doc": a reader must be able to check a
  claim in one jump.
- **Status lives in the audit — *and in the brief's own first screen*.** When a brief ships, flip the
  audit's Status column in the same PR. **Amended 2026-09-06:** *"one place to look"* is what let six of
  sixteen briefs drift — a build agent opens the **brief**, not the audit, so a brief with no marker (or a
  stale one) at the top is read as a plan to execute. Every brief now carries its state in its own first
  screen, **cited to `file:line` in the code**, and keeps the superseded claim visible rather than
  silently overwriting it — a silently-updated marker teaches the next reader nothing about how often
  these drift.

- [`grounding-gate-unrun.md`](grounding-gate-unrun.md) — **the retrieval fix is proven, the answer-quality claim one layer up is not.** Blocked on a multi-hour gateway 503, not on the work.

---
📖 [Docs index](../../README.md) · [Architecture index](../README.md) · [OpenMoxie feature audit](../openmoxie-feature-audit.md) · [Implementation plan](../implementation-plan.md)
