# 🛠️ scripts

Repo-maintenance helpers.

- `check-doc-links.py` — verify every internal markdown link resolves (keeps the docs navigable).
  Run from the repo root: `python3 scripts/check-doc-links.py`.
